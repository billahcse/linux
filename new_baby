Tools you must needs:

sudo apt-get update
sudo apt-get upgrade -y
sudo apt-get install git -y
sudo apt-get install make -y
sudo apt-get install g++ -y
sudo apt-get install wget -y


REALTEK(RTL8821CE) wifo driver installation process:

$ sudo lshw -class network // Check the networkdriver info
$ sudo apt update sudo apt install -y dkms git 
$ git clone https://github.com/tomaspinho/rtl8821ce.git
$ cd rtl8821ce 
$ sudo ./dkms-install.sh 
$ sudo modprobe 8821ce  // if you got error then check be sure that Secure Boot must is disable.


MiniConda installation process:

$    wget https://repo.continuum.io/miniconda/Miniconda2-latest-Linux-x86_64.sh
$    Miniconda2-latest-Linux-x86_64.sh
$    conda
$    cat ~/.bashrc
$    export PATH="/home/user_name/miniconda2/bin:$PATH"
$    conda
$    conda config --add channels defaults
$    conda config --add channels bioconda
$    conda config --add channels conda-forge
$    conda install -c bioconda maker

Anaconda installation process:
$   wget https://repo.continuum.io/archive/Anaconda3-2018.12-Linux-x86_64.sh
$   bash Anaconda3-2018.12-Linux-x86_64.sh
$   $    export PATH="/home/user_name/anaconda3/bin:$PATH"

Spyder:
$ conda update spyder
$ conda install pyopengl

opencv
pip install opencv-python
numpy
pip install numpy

pip3:
sudo apt install python3-pip

scikit-learn
pip3 install -U scikit-learn


gpu
$   nvidia-smi
$ 

tensorflow1.1.15
$   conda create -n tensorflow_1_15
$   conda activate tensorflow_1_15
$   sudo apt install python-pip
$   python -m pip install --upgrade pip setuptools
$   mbillah@mbillah:~$ pip install tensorflow==1.15

Labelme:
conda create --name=labelme python=2.7
conda activate labelme
conda install pyqt
pip install labelme
conda install scikit-image

LabelImg:
conda create --name labelimg
pip3 install labelImg

Error fixed

pip uninstall pyqtwebengine
pip install pyqtwebengine

more then 0.16
pip install scikit-image
pip install scikit-image
conda install -c conda-forge scikit-image
